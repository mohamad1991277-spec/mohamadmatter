# دليل نشر منصة إحصائيات المواطنين

لرفع الموقع بشكل مجاني وسليم، سنستخدم أفضل خدمات متاحة حالياً لـ Next.js:
1. **Vercel**: لاستضافة الموقع (Frontend & Backend).
2. **Neon**: لاستضافة قاعدة البيانات (PostgreSQL) مجاناً.

---

### الخطوة 1: تجهيز قاعدة البيانات (Neon)
1. ادخل على موقع [neon.tech](https://neon.tech) وسجل حساباً مجانياً.
2. أنشئ مشروعاً جديداً (Create Project).
3. سيظهر لك رابط الاتصال (Connection String) يشبه هذا:
   `postgres://user:password@ep-cool-site.us-east-2.aws.neon.tech/neondb?sslmode=require`
4. انسخ هذا الرابط واحتفظ به.

---

### الخطوة 2: تحديث إعدادات المشروع
قبل الرفع، نحتاج لتغيير نوع قاعدة البيانات من `sqlite` إلى `postgresql`.

1. افتح ملف `prisma/schema.prisma` في مشروعك.
2. غير السطر:
   ```prisma
   provider = "sqlite"
   ```
   إلى:
   ```prisma
   provider = "postgresql"
   ```
3. احفظ الملف.

---

### الخطوة 3: رفع الكود إلى GitHub
1. أنشئ مستودع (Repository) جديد على GitHub.
2. ارفع ملفات المشروع إليه.

---

### الخطوة 4: النشر على Vercel
1. اذهب إلى [vercel.com](https://vercel.com) وسجل الدخول بحساب GitHub.
2. اضغط **Add New > Project**.
3. اختر مستودع مشروعك (Import).
4. في قسم **Environment Variables**، أضف متغيراً جديداً:
   - **الاسم (Key)**: `DATABASE_URL`
   - **القيمة (Value)**: انسخ رابط قاعدة البيانات من الخطوة 1.
5. اضغط **Deploy**.

---

### مبروك! 🎉
سيعمل الموقع الآن أونلاين، وسيتم حفظ البيانات بشكل دائم وآمن على السحابة ولن يتم حذفها.
